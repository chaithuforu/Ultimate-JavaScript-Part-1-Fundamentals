// Function to get the maximum of two numbers.
function findMaxNumber(a, b) {
    return (a > b) ? a : b;
}
let number = findMaxNumber(10, 2);
// Log the max number.
console.log(number);
console.log(findMaxNumber(5, 2));
console.log(findMaxNumber(90, 100));